# Brano in ascolto accanto al selettore di lingua (src/lib/now-playing.ts).
# Copia questo file in .env.local — è già ignorato da git — e riempi i due valori.
# Senza, la riga semplicemente non compare: il sito funziona lo stesso.
#
# 1. collega Spotify a Last.fm: https://www.last.fm/settings/applications
# 2. crea una chiave API: https://www.last.fm/api/account/create
VITE_LASTFM_USER=
VITE_LASTFM_KEY=
